const listAnimals = require("./dom/DisplayCatalog")

// Display a list of all animals
listAnimals()


// The only modules needed on main.js are ones that will display what the USER WILL SEE FIRST. Other modules need to be broken up and put in the appropriate js files

// APIManager.getAllAnimals().then(allAnimals => console.log(allAnimals))